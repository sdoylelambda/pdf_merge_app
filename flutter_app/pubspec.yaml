name: pdf_merge_app
description: A simple PDF merger
version: 0.0.1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  file_picker: ^10.3.3
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter